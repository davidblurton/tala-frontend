import {
  objectEach
} from '../../../helpers/object-each';
import { module, test } from 'qunit';

module('ObjectEachHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = objectEach(42);
  assert.ok(result);
});
